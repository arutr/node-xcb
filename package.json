{
  "name": "node-xcb",
  "version": "0.0.0",
  "description": "XCB bindings for Node.js",
  "engines": {
    "node": ">= 10.13 <11"
  },
  "devDependencies": {
    "@types/jest": "^23.3.12",
    "@types/node": "^10.12.18",
    "bindings": "^1.5.0",
    "jest": "~23.6.0",
    "node-addon-api": "^1.6.3",
    "rimraf": "^2.6.3",
    "ts-jest": "~23.10.5",
    "tslint": "~5.12.0",
    "tslint-microsoft-contrib": "~6.0.0",
    "tsutils": "^3.10.0",
    "typescript": "^3.4.2"
  },
  "scripts": {
    "clean": "rimraf coverage build tmp",
    "build": "tsc -p tsconfig.release.json",
    "dev": "tsc -w -p tsconfig.release.json",
    "lint": "tslint -t stylish --project \"tsconfig.json\"",
    "test": "jest --coverage",
    "test:watch": "jest --watch"
  },
  "author": "Artur Komoter",
  "license": "MIT",
  "dependencies": {
    "tslib": "~1.9.3"
  }
}
